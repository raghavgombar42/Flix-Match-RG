node_modules
dist
dist-ssr
*.local
.env
.env.local
.env.*.local

# Secrets accidentally saved as plain notes — never commit these regardless of name.
API keys.txt
*api*key*.txt
*API*Key*.txt

# Build artifacts
*.tsbuildinfo

# Personal / course reference material — not app source, kept local only.
*.pdf

# Supabase CLI local cache (per-machine project link, not project source).
supabase/.temp

# Vercel CLI local cache (per-machine project link, not project source).
.vercel

# Editor
.vscode/*
!.vscode/extensions.json
.idea

# OS
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
